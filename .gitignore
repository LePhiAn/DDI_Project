# Bỏ qua dữ liệu thô và đã xử lý (vì rất nặng)
data/raw/
data/processed/

# Bỏ qua môi trường ảo
.venv/
venv/
__pycache__/
*.pyc

# Mô hình: Chỉ bỏ qua nếu file > 100MB (GitHub limit)
# models/r_gcn_final_model.pth  # Bỏ comment nếu file này > 100MB